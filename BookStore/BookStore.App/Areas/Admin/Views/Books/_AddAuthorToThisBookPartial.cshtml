@model BookStore.Models.ViewModels.Book.BookDetailsViewModel

<div class="col-md-6">
    <button type="button" class="btn btn-default" data-toggle="modal" data-target="#add-author">
        Add Author
    </button>
    <hr />
    <div class="modal fade" id="add-author" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header text-center">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h1 class="modal-title text-center text-danger"><span class="glyphicon glyphicon-book"></span></h1>
                </div>
                <div class="modal-body">
                    <div class="modal-div">
                        <p class="text-danger">Add Author To This Book</p>
                    </div>
                    <hr />
                    <div class="modal-div">
                        @using (Html.BeginForm("AddAuthorToBook", "Books", FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                            @Html.HiddenFor(model => model.Id)

                            <div class="col-md-6">
                                @Html.LabelFor(model => model.SelectAuthors, htmlAttributes: new { @class = "control-label" })
                                <div class="form-group">
                                    @Html.DropDownList("SelectAuthors", Model.SelectAuthors as SelectList, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(model => model.SelectAuthors, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-actions no-color text-center col-md-6">
                                <input type="submit" value="Add" class="btn btn-default" />
                            </div>
                        }
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                </div>
            </div>
        </div>
    </div>
</div>








